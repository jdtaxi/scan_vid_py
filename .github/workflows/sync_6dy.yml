name: Sync 6dy

on:
  schedule:
    - cron: '0 0 * * *' # 每天北京时间早上 8 点运行
  workflow_dispatch:      # 支持手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # 必须赋予写权限
      
    steps:
      - name: Checkout My Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync Function Folder
        run: |
          git clone --depth 1 https://github.com/6dylan6/jdpro.git temp_upstream
          mkdir -p function
          cp -rf temp_upstream/function/* ./function/
          
          # 关键修改：改用 .cjs 后缀
          cd function
          [ -f dylib ] && cp dylib dylib.cjs || echo "dylib not found"
          [ -f dylans ] && cp dylans dylans.cjs || echo "dylans not found"
          cd ..
          
          rm -rf temp_upstream

      - name: Push Changes
        run: |
          git add function/
          # 检查是否有变更。--quiet 如果没有差异则返回 0
          if git diff --staged --quiet; then
            echo "No changes detected, skipping commit."
          else
            git commit -m "Automated Sync: update function folder and add .js suffixes"
            git push origin main
          fi
